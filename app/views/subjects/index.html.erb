<% content_for :title, "Предмети - Школа Aronnax" %>

<div class="text-center mb-12">
  <h1 class="text-5xl md:text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-600 to-pink-600 mb-4 animate-pulse">
    Обирай свій предмет! 🎓
  </h1>
  <p class="text-2xl text-gray-700">Почни навчання прямо зараз!</p>
</div>

<% if @subjects.empty? %>
  <div class="text-center py-12">
    <div class="text-6xl mb-4">📚</div>
    <p class="text-2xl text-gray-600">Поки що немає предметів</p>
  </div>
<% else %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <% @subjects.each do |subject| %>
      <%= link_to subject_path(subject), class: "group" do %>
        <div class="bg-white rounded-3xl shadow-xl hover:shadow-2xl transform hover:scale-105 transition-all duration-300 overflow-hidden border-4 border-transparent hover:border-purple-400">
          <!-- Colorful header -->
          <div class="bg-gradient-to-r from-yellow-400 via-pink-400 to-purple-500 h-24 flex items-center justify-center">
            <div class="text-white text-6xl animate-bounce">
              <%= subject.name.include?('Буквар') ? '📖' : '📚' %>
            </div>
          </div>

          <!-- Content -->
          <div class="p-6">
            <h2 class="text-2xl font-bold text-gray-800 mb-2 group-hover:text-purple-600 transition-colors">
              <%= subject.name %>
            </h2>

            <div class="flex items-center space-x-2 text-gray-600 mt-4">
              <span class="text-2xl">📑</span>
              <span class="text-lg">
                <%= pluralize(subject.chapters.count, 'розділ', 'розділи', 'розділів') %>
              </span>
            </div>

            <!-- Fun button -->
            <div class="mt-6">
              <span class="inline-block bg-gradient-to-r from-purple-500 to-pink-500 text-white px-6 py-3 rounded-full font-bold text-lg group-hover:from-purple-600 group-hover:to-pink-600 transition-all">
                Почати навчання! →
              </span>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
<% end %>

<!-- Fun decorative elements -->
<div class="mt-12 text-center">
  <div class="inline-flex space-x-4 text-5xl animate-bounce">
    <span>⭐</span>
    <span style="animation-delay: 0.1s;">🌟</span>
    <span style="animation-delay: 0.2s;">✨</span>
  </div>
</div>
