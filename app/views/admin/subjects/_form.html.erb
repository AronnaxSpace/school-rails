<%= form_with(model: [:admin, subject], class: "space-y-4") do |form| %>
  <% if subject.errors.any? %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
      <h3 class="font-bold mb-2"><%= pluralize(subject.errors.count, "помилка", "помилки", "помилок") %>:</h3>
      <ul class="list-disc list-inside">
        <% subject.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, "Назва предмету", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.text_field :name, class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent", placeholder: "Наприклад: Буквар (Ukrainian ABC)" %>
  </div>

  <div class="flex space-x-4">
    <%= form.submit "Зберегти", class: "bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg font-semibold transition-colors cursor-pointer" %>
    <%= link_to "Скасувати", :back, class: "bg-gray-500 hover:bg-gray-600 text-white px-6 py-2 rounded-lg font-semibold transition-colors" %>
  </div>
<% end %>
